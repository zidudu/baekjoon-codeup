//나중에 한번 더풀기!
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	// A, B, V
	int A,B,V;
	//입력
	scanf("%d %d %d", &A, &B, &V);

	//올라간 총합
	int sum = 0;
	
	//낮엔 A미터 올라가고 밤엔 B미터 미끄러짐 +a -b
	//낮에 올라갈때 V값과 같아지면 끝남
	//낮에 올라가고 내려갈땐 날짜가 +1됨. 즉 하루에 올라감
	/// - B - 1 을 하는 경우 달팽이가 올라가는 땐 내려가지 않아도 다 올라간 것이니
	// -1 과 -B(내려가지 않았으니)를 V에 빼줌 
	// +1은 올라간 날짜를 포함해야 해서
	//묶어서 계산을 해도 됨
	sum = (V - B - 1) / (A - B) + 1;

	// 2 1 5
	// 5 - 1 - 1 = 3 / (2 - 1) + 1
	//미터 나누기 sum = 몫 == 며칠
	

	printf("%d", sum);
	//마지막 올라간 것도 올라
}
