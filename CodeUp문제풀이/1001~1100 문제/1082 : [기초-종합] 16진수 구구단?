#define _CRT_SECURE_NO_WARNINGS_
#include <stdio.h>
int main() {
    //16진수로 한자리수 입력
    int a;
    scanf("%x", &a);
    //구구단 출력
    for (int i = 1; i < 16; i++) {
        printf("%X*%X=%X\n", a, i, a * i);
    }

}
