#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	//a , b 
	int list[10];
	//입력
	for (int i = 0; i < 10; i++) {
		scanf("%d",&list[i]);
	}
	//5의 배수를 하나만 출력
	//없으면 0을 출력
	for (int i = 0;a i < 10; i++) {
		if (list[i] % 5 == 0) {

			printf("%d", list[i]);
			return 0;
		}
		
	}
	printf("0");

}
