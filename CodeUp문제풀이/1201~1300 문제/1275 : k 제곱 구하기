#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(){
int n, k;
scanf("%d %d",&n,&k);
int sum = 1;
//n의 값에 k번 곱해야 함. k=3이면 1,2,3 해야함.
//n의 값에 0을 곱하면 1이됨.
//이 원리는 
for(int i=0;i<k; i++){
sum *= n;
}
printf("%d",sum);
}
