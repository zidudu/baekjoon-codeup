#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int n;
    scanf("%d",&n);

    //약수 구하기
    for(int i=1;i<=n;i++){
        if(n%i == 0) printf("%d ",i);
    }

    
    return 0;
}
