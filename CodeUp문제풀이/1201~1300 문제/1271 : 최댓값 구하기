#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	//정수(n)
	int n;
	int a;
	int max = 0;
	//입력
	scanf("%d",&n);
	if (n > 1000) return 0;
	//n개의 정수에 입력
	for (int i = 0; i < n; i++) {
		scanf("%d", &a);
		if (max < a) max = a;
	}
	//출력
	printf("%d", max);
}
