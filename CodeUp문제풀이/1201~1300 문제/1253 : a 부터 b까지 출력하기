#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX
int main() {
	int a,b;
	int max, min;
	scanf("%d %d", &a, &b);

	if (a > b) { max = a; min = b; }
	else {  max = b;  min = a; }

	for (int i = min; i <= max; i++) {
		printf("%d ", i);
	}
}
