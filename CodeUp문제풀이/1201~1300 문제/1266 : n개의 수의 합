#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	//n
	int n;
	int a;
	int sum=0;
	//입력
	scanf("%d",&n);
	//n개의 정수 입력 후 합하기
	for (int i = 0; i < n; i++) {
		scanf("%d", &a);
		sum += a;
	}
	//값 출력
	printf("%d", sum);
}
