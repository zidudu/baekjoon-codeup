#include <stdio.h>
int main() {
	float a,b;
	float extent;
	//밑변 높이 받기
	scanf("%f %f", &a, &b);
	//넓이 계산
	extent = (a * b) / 2;
	//넓이를 소수 첫째자리까지만 출력
	printf("%0.1f", extent);
	return 0;
}
//해결책 => float를 써야함. 이유는 모름

//삼각형의 넓이를 구하는 프로그램을 작성한다.
//
//삼각형의 넓이 = 밑변 * 높이 / 2
// 밑변(a)과 높이(b)가 정수로 입력된다.
//삼각형의 넓이를 소수 첫째자리까지 출력한다.
// 정수 끼리의 연산 결과 = 정수

//실수 끼리의 연산 결과 = 실수
//
//정수와 실수의 연산 결과 = 실수
