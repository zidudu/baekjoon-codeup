#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
	//학년,반,번호
	int Grade,Class,num;
	//학번
	int ID;
	//입력
	scanf("%d %d %d", &Grade,&Class,&num);
	if (Grade > 3 || Class > 6 || num > 30) return 0;
	//모두 더하기
	ID = (Grade * 1000) + (Class * 100) + num;
	//출력
	printf("%d", ID);

}
/*
===배열로 만들어 출력하기===
	int a[10];
	a[0] = Grade;
	a[1] = Class;
	a[2] = num / 10;
	a[3] = num % 10;
	for (int i = 0; i < 4; i++) {
		printf("%d", a[i]);
	}
	=> 이 방법도 되게 간편하면서 신기하면서 뭔가뭔가다
=================================
*/


//학번을 입력 받아 다음 형식으로 출력하시오.
//
//학번은 학년, 반, 번호로 입력된다.
//
//예)
//
//2 3 27   ==== = > 2327로 출력
//
//2 3 7   ======> 2307로 출력(번호가 10번미만이면 0을 붙여 출력)
//
//입력
//학년, 반, 번호가 공백을 기준으로 입력으로 주어진다. (정수)
//
//입력범위:
//
//학년: 3이하, 반, 6이하, 번호, 30이하
//
//출력
//학번을 붙여서 출력한다.번호가 10번 미만일때는 0을 붙여 출력한다.
