#include <stdio.h>
int main() {
	//입력받는 초 변수 
	int seconds;
	//분 변수
	int m = 0;
	// 초 입력
	scanf("%d", &seconds);
	// 초를 분 초의 형태로 변환하기
	while (seconds >= 60) {
		seconds = seconds - 60;
		++m;
	}
	//출력
	printf("%d %d", m,seconds);
	


}
//초를 입력받아 분 / 초의 형태로 출력하시오.
//
//예)
//
//60  ====>   1 0    (1분 0초를 뜻함)
//
//70    ====>    1  10       (1분 10초를 뜻함)
//입력
//초가 입력된다.(자연수)
//
//출력
//분, 초 순서로 출력한다.
