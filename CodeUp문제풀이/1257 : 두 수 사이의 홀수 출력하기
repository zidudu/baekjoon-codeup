#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX
int main() {
	//a , b 
	int a,b;
	//입력
	scanf("%d %d", &a, &b);
	//a가 짝수인지 홀수인지 계산
	//홀수이면
	if (a % 2 == 1) {
		for (int i = a; i <= b; i = i + 2) printf("%d ", i);
	}
	else {
		for (int i = a + 1; i <= b; i = i +2) printf("%d ", i);

	}
	//시작수부터 마지막 수까지의 모든 홀수 출력
	
}
